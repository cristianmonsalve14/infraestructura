# Archivo histórico

Material conservado por referencia. **No usar en el desarrollo actual.**

| Carpeta / archivo | Motivo |
|-------------------|--------|
| `ddl/initial_schema.sql` | BD monolítica legada (antes de database-per-service) |
| `docs/*.docx` | Copias Word; la fuente actual son los `.md` en `docs/` |
| `registro_usuario_fullstack3.md` | Snapshot de registro (abril 2026), desactualizado |

Scripts, diagramas y documentación vigentes están en `ddl/`, `diagrams/`, `docs/` y `authService/diagrams/`.
